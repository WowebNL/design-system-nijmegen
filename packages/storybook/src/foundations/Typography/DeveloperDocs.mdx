import { Meta } from '@storybook/addon-docs';

<Meta title="foundations/typography/Developer documentation"/>
# Typography developer documentation
All typography styling and loading of the fonts is setup in the `@gemeentenijmegen/typography` package.

## Theming
Default typography styling will be applied if you load the package and setup the `nijmegen-theme` class. All default html elements will be styled.
